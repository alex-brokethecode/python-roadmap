# Create and Import a Module: Create a module named my_utils.py with a function that calculates the square of a number.
# Then, create a script that imports this module and uses the function.

from my_utils import square

print(square(4))
