# __name__ Variable Usage: Create a module named calculator.py with functions for addition, subtraction, multiplication, and division.
# Add a conditional statement that prints the results of some calculations only when the module is executed directly (not when it's imported).

import calculator
